-- =====================================================================
-- ถอดฟีเจอร์ "ช่องทางชำระเงินที่บันทึกไว้" ออก
--
-- หน้า /account/payment ให้ลูกค้าบันทึก บัตรเครดิต / เก็บเงินปลายทาง / โอนธนาคาร
-- แต่ระบบรับชำระผ่าน PromptPay QR + ตรวจสลิป อย่างเดียว (orders.payment_method
-- ถูกฮาร์ดโค้ดเป็น 'promptpay') และหน้า checkout ไม่เคยอ่านตารางนี้เลยสักครั้ง
-- => เป็นฟีเจอร์ที่ทำให้ลูกค้าเข้าใจผิดว่าเลือกวิธีจ่ายเงินได้
--
-- ตารางนี้ไม่มีข้อมูลอยู่เลย (0 แถว) ตอนถอดออก
-- ถ้าวันหน้ารองรับหลายช่องทางจริง ค่อยสร้างใหม่ให้ตรงกับที่ระบบทำได้
-- (โครงเดิมดูได้ใน migration 20260701185434_account_section.sql)
-- =====================================================================
drop table if exists public.payment_methods;
